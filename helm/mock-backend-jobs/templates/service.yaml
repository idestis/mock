apiVersion: v1
kind: Service
metadata:
  name: {{ include "mock-backend-jobs.fullname" . }}
  labels:
    {{- include "mock-backend-jobs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "mock-backend-jobs.selectorLabels" . | nindent 4 }}
